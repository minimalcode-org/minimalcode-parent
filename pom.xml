<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.minimalcode</groupId>
    <artifactId>minimalcode-parent</artifactId>
    <version>0.5</version>
    <packaging>pom</packaging>

    <parent>
        <groupId>org.sonatype.oss</groupId>
        <artifactId>oss-parent</artifactId>
        <version>9</version>
    </parent>

    <name>minimalcode-parent</name>
    <description>Parent pom for all minimalcode sub-projects</description>
    <url>http://github.com/minimalcode-org/</url>
    <inceptionYear>2015</inceptionYear>

    <organization>
        <name>Minimalcode</name>
        <url>http://minimalcode.org/</url>
    </organization>

    <licenses>
        <license>
            <name>The Apache Software License, Version 2.0</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>dewos</id>
            <name>Fabio Piro</name>
            <email>pirofabio@gmail.com</email>
        </developer>
    </developers>

    <scm>
        <url>https://github.com/minimalcode-org/minimalcode-parent</url>
        <connection>scm:git:git@github.com:minimalcode-org/minimalcode-parent.git</connection>
        <developerConnection>scm:git:git@github.com:minimalcode-org/minimalcode-parent.git</developerConnection>
    </scm>

    <properties>
        <java.version>1.6</java.version>
        <junit.version>4.12</junit.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>

    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.1</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>
        </plugins>
    </build>
    
</project>